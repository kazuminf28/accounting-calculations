<h1> ユーザーの追加 </h1>

<%= form_with model: @user do |f| %>

    <div>
      <%= f.label :name, "名前" %>
      <%= f.text_field :name %>
    </div>

    <div>
      <%= f.submit "作成" %>
    </div>
<% end %>

<%= link_to "戻る", users_path %>